[pytest]
markers =
  slow: marks tests as slow (deselect with '-m "not slow"')
  subprocess: marks tests that invoke real subprocesses (e.g. Claude CLI, git)
  locale: marks tests that are locale-sensitive (TZ / LC_ALL / encoding)
