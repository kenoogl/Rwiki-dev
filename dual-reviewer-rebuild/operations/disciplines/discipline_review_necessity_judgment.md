---
name: レビュー所見の必要性判定ステップ
description: 主役レビューが検出した所見を「修正必須 / 必要に応じて / 修正不要」の三段に分類する独立の判定ステップ。主役の過剰修正偏りを抑制する装置。
type: feedback
---

> 用語：主役・敵対役・判定役の定義は `CONVENTIONS.md` 節 8 を正本とする。

主役レビューが検出した所見について、修正を実施する前に「修正の必要性」を独立に判定するステップを組み込む。判定は別エージェントが行い、5 つの観点で各所見を評価したうえで、5 つの条件に従って三つのラベル（修正必須 / 必要に応じて / 修正不要）に分類する。

**Why:** 主役レビューは「不備を見落とすな」という方向に最適化されており、検出した所見を採用する側に偏る傾向（過剰修正偏り）がある。検出と判定を別エージェントに分け、判定側に「採用しなければならない理由」を要件として課すことで、この偏りを抑制する。検出側の感度は落とさずに済むため、見逃しは増えない。

**How to apply:**

## 全体の流れ

四つの段階で進める。

- 段階 A（主役による一次検出）：主役エージェントが多重の検査軸で所見を列挙する。
- 段階 B（敵対役による独立検証）：別エージェントが独立に検出し、主役が見落とした所見を補い、主役所見の修正動機を試問する。
- 段階 C（判定役による必要性判定）：さらに別エージェントが各所見を 5 観点で評価し、5 条件で分類する。
- 段階 D（統合と利用者提示）：統合役が判定結果に従い、三ラベル別に利用者へ提示する。

## 5 観点の評価（判定役の出力に必須）

各所見に次の 5 つの値を付与する。

- `requirement_link`：要件と直接対応する／関連あり／関連なし
- `ignored_impact`：致命／機能阻害／劣化／軽微
- `fix_cost`：高（複数フィーチャーや構造変更）／中／低
- `scope_expansion`：要件範囲を広げる／広げない
- `uncertainty`：判断が割れる／中程度／明確
  - 23 パターン（別ファイル `discipline_review_judgment_patterns.md`）に該当した所見は、原則として `uncertainty` を「判断が割れる」または「中程度」と設定し、「明確」には分類しない。

主観的判断のばらつきを抑えるため、半機械的な対応規則を判定役のプロンプトに埋め込む。例外を選ぶ場合は、その理由を `override_reason` として明記する。

## 5 条件の判定ルール（順に評価、強い条件が優先）

1. 致命的影響あり → 修正必須
2. 要件と直接対応 かつ 影響が機能阻害以上 → 修正必須
3. 要件範囲を広げる かつ 致命的でない → 修正不要 もしくは 利用者判断へ送る
4. 修正コスト > 無視した場合の影響 → 修正不要寄り
5. 判断が割れる → 利用者判断（「必要に応じて」ラベルに分類し、推奨アクションを「利用者判断」とする）

## 三ラベルの提示方式

- 修正必須：原則として一括適用。利用者は念のため確認したい所見だけ個別に見直す。
- 修正不要：原則として一括見送り。利用者には一覧で示し、異議申し立ての機会を残す。
- 必要に応じて：全件を利用者に提示し、「全件適用 / 全件見送り / 個別に判断」の三択を取る。

## 適用する場面

- 設計フェーズのレビュー
- 要件フェーズのレビュー
- 複数フィーチャーにまたがる整合性確認の場面

## 例外的な扱い

判定役が「修正必須」と分類した所見について、主役が再検討の結果「誤検出」と判断する場合がある。その場合は、主役の注記として「誤検出の可能性」を明示し、利用者個別レビューで見送るかどうかを最終決定する。

**関連する規律：**

- レビュー判定の 23 パターン（`discipline_review_judgment_patterns.md`）— 検出段階のチェックリスト。本必要性判定の前段として、検出時に該当判定し、該当所見の `uncertainty` を高く扱う。
- 所見の四要素（`discipline_finding_4elements.md`）— 検出所見を事実として記録する場面の観点規律。
- レビュー所見のテンプレート（`discipline_review_log_template.md`）— 利用者に判断を仰ぐ場面の形式規律。三ラベル別の提示の並びにも適用する。
