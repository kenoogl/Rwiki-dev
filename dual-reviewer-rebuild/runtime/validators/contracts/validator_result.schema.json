{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "urn:dual-reviewer:foundation:validator_result:1.0.0",
  "title": "validator_result",
  "description": "validation 実行結果。raw evidence を書き換えず別 artifact として重ねる（Requirement 6 受入 3、Interface Decision 4、design §8）。",
  "type": "object",
  "additionalProperties": true,
  "required": ["run_id", "validator_status", "checked_contract", "error_list", "validated_by", "validated_at"],
  "properties": {
    "run_id": { "type": "string" },
    "validator_status": {
      "type": "string",
      "description": "metadata_contract.yaml の canonical 語彙と整合（再定義しない）。",
      "enum": ["not_run", "passed", "failed", "blocked"]
    },
    "checked_contract": { "type": "string", "description": "検証した schema/metadata contract の対象範囲" },
    "error_list": { "type": "array", "items": { "type": "object" }, "description": "契約違反一覧（passed 時は空）" },
    "validated_by": { "type": "string" },
    "validated_at": { "type": "string" }
  },
  "x-raw-evidence-rule": "raw evidence は immutable。validation 結果は別 artifact として重ねるのみ（Interface Decision 4）。"
}
