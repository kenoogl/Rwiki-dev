# Structure Steering

- `intent/` and `operations/` are upstream to specs.
- `.kiro/specs/` is the SSoT for feature-level development.
- `runtime/`, `experiments/`, `learning/`, and `paper/` are downstream artifact areas.
