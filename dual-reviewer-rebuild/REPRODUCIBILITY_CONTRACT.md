# REPRODUCIBILITY_CONTRACT

Each run must record:

- protocol version
- prompt version
- runtime version
- target artifact hash
- treatment
- model configuration
- operator sign-off status

Raw evidence is immutable. Repairs and reanalysis must produce separate derived artifacts.
