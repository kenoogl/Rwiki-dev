# requirements alignment template

> 複数 feature spec の `requirements.md` の整合性を確認する alignment gate のテンプレート。各 alignment area で findings を残し、gate 承認の根拠とする。

## 1. alignment scope

- alignment type: `requirements-alignment-gate`
- aligned feature set:
  - `<feature A>`
  - `<feature B>`
  - `<feature C>`
- alignment date:
- reviewed documents:
  - `<feature A>/requirements.md`
  - `<feature B>/requirements.md`
  - `<feature C>/requirements.md`

## 2. alignment areas

requirements alignment では次の領域を横断確認する。

### 2.1 metadata contract

- finding:
- recommended action:

### 2.2 invalidation rule

- finding:
- recommended action:

### 2.3 prompt / schema dependency

- finding:
- recommended action:

### 2.4 responsibility boundary

- finding:
- recommended action:

## 3. metric snapshot

- alignment findings count:
- blocking alignment findings count:
- cross-feature contract gaps:

## 4. gate readiness

- readiness:
- remaining open points:
- requested human decision:
